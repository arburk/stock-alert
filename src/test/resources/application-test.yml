spring:
    mail:
      properties:
        mail:
          debug: true

logging:
  level:
    com.github.arburk.stockalert: DEBUG
    feign.Logger: DEBUG
    feign.Request: DEBUG
    feign.Response: DEBUG
    org.springframework.mail.javamail: DEBUG
